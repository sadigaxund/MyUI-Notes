# TODO

## Editor behaviors

- [ ] VSCode-style preview tabs: opening a file that was never edited makes a
      "preview" tab; opening another file replaces that untouched preview tab.
      Once the preview tab is edited (or opened again?), it becomes a
      permanent tab. Only genuinely edited tabs accumulate.

## Components

- [ ] Track the my-you-eye TreeView feature request
      (https://github.com/sadigaxund/my-you-eye/issues/1) — selection
      callback, per-file-type icons, icon overrides. Replace the custom
      `FileTree` component with the library component once available.
