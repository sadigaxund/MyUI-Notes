# vsnote workspace

This workspace is baked into the image.  Open it in your browser and start
editing — the terminal, debugger, and task runners are disabled in this build;
only the editor, file explorer, search, and settings are available.
